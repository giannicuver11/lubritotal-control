[build]
  command = "npx prisma generate && npm run build && mkdir -p .next/prisma && cp prisma/dev.db .next/prisma/dev.db"
  publish = ".next"

[[redirects]]
  from = "/api/*"
  to = "/api/:splat"
  status = 200

[build.environment]
  NODE_VERSION = "20"
